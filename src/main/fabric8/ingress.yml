apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ${project.artifactId}
spec:
  rules:
  - host: ${project.artifactId}.iris.seli.gic.ericsson.se
    http:
      port: 8090
      paths:
      - backend:
          serviceName: ${project.artifactId}
          servicePort: 8090
        path: /
